{
  "$id": "similar.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Similar resources",
  "description": "URI or ISBN for a similar resource (parallel edition etc.)",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "id": {
        "type": "string",
        "format": "uri"
      },
      "label": {
        "type": "string"
      },
      "note": {
        "$ref": "note.json"
      },
      "isbn": {
        "$ref": "isbn.json"
      }
    },
    "required": [
      "label"
    ]
  }
}
